---
"vimo-oss": patch
---

Sync release: republish the launcher so `npm i -g vimo-oss` carries the current generation (self-updating launcher, forced app refresh, visible versions). No launcher code changes — this bump is the update signal for existing installs.
